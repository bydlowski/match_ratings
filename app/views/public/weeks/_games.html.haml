.col.xs-col-9.xs-p2
  .col.xs-col-12.xs-text-center
    .flex.xs-flex-justify-center{style:'flex-wrap: wrap;'}
      - game_counter = 0
      - @stats.order(game_date: :asc,game_period: :asc,game_time: :asc).each do |game|
        .col.xs-col-2.xs-border.xs-m1.fill-white{style:'min-width: 130px;'}
          .col.xs-col-12.xs-border-bottom-lighter.xs-p1
            = game.game_date
            = surround '(', ')' do
              = game.game_time
          .col.xs-col-12.xs-border-bottom-lighter.xs-p1.xs-relative
            .col.xs-col-5
              .logo-teams.logo-tiny{class: "logo#{game.away_team_abrev}"}
              = game.away_team_abrev
              - if session[:value] == 'Show'
                %br
                = surround '(', ')' do
                  = team_stats_week(game.away_team_abrev,week)
            .col.xs-col-2.xs-absolute.xs-mx-auto.xs-l0.xs-r0.xs-pt1{style:'width: 50px!important;font-size: 200%;color:rgba(0,0,0,0.7);'}
              %p= '@'
            .col.xs-col-5.xs-float-right
              .logo-teams.logo-tiny.logo-flip{class: "logo#{game.home_team_abrev}"}
              = game.home_team_abrev
              - if session[:value] == 'Show'
                %br
                = surround '(', ')' do
                  = team_stats_week(game.home_team_abrev,week)
          .col.xs-col-12.xs-relative
            = score(game.game_date,game.game_time,game,game_counter)
            - game_counter += 1
   
%script
  $('.hid0').on('click', function() {
  $('.hid0').toggleClass('hidden-rating');
  $('.hidt0').toggle();
  })
  $('.hid1').on('click', function() {
  $('.hid1').toggleClass('hidden-rating');
  $('.hidt1').toggle();
  })
  $('.hid9').on('click', function() {
  $('.hid9').toggleClass('hidden-rating');
  $('.hidt9').toggle();
  })
  $('.hid10').on('click', function() {
  $('.hid10').toggleClass('hidden-rating');
  $('.hidt10').toggle();
  })
  $('.hid11').on('click', function() {
  $('.hid11').toggleClass('hidden-rating');
  $('.hidt11').toggle();
  })
  $('.hid12').on('click', function() {
  $('.hid12').toggleClass('hidden-rating');
  $('.hidt12').toggle();
  })
  $('.hid13').on('click', function() {
  $('.hid13').toggleClass('hidden-rating');
  $('.hidt13').toggle();
  })
  $('.hid14').on('click', function() {
  $('.hid14').toggleClass('hidden-rating');
  $('.hidt14').toggle();
  })
  $('.hid15').on('click', function() {
  $('.hid15').toggleClass('hidden-rating');
  $('.hidt15').toggle();
  })
